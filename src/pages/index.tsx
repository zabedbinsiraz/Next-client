import HomeComponent from "../components/Home/Home";

const Home = () => {
  return (
    <>
      <HomeComponent />
    </>
  );
};

// Home.getInitialProps = async (ctx: NextPageContext ) => {

// };

export default Home;
